apply plugin: 'java'

dependencies {
    compile fileTree (dir : "lib", include: "*.jar")
}

task displayJars {
    doLast {
        println "${configurations.compile.collect {File f -> f.name}}"
    }
}

